stages:
- name: Build and Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: evertonaulian/node:${CICD_EXECUTION_ID}
      pushRemote: true
      registry: index.docker.io
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: evertonaulian/node
      pushRemote: true
      registry: index.docker.io
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
timeout: 60
notification:
  recipients:
  - recipient: '#rancher'
    notifier: c-trw7c:n-l2z7l
  condition:
  - Success
